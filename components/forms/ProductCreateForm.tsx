"use client";

/** Canonical form UI lives in CaseCreateForm.tsx (Soft Nav module identity). */
export {
  CaseCreateForm as ProductCreateForm,
  CaseCreateForm,
} from "@/components/forms/CaseCreateForm";
